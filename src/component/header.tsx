import React from 'react';
import './header.css';

const Header: React.FC = () => {
    return (
        <header className="header">
            รายการของฉัน
        </header>
    );
};

export default Header;
